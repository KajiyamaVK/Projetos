import {StyleSheet} from 'react-native'

const style = StyleSheet.create({
    search:{
        height: 40, 
        borderColor: 'gray', 
        borderWidth: 1,
        marginTop:50,
        margin:10
    },
})

export default style;